#!/usr/bin/make -f

%:
	dh $@ --with python2

override_dh_auto_clean:
	rm -rf *.egg-info
	find . -name "*.pyc" | xargs rm -f
	rm -rf controller/ docs/ .gitignore LICENSE.txt README.md
	mv -v pykeg/* .
	rm -rf pykeg/

